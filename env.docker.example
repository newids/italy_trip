# Database Configuration for Docker
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/italy_trip"

# Docker Container Configuration (should match docker-compose.yml)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=italy_trip
